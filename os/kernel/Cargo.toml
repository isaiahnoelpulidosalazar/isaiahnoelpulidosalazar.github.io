[package]
name = "inpsos_kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
# Required for standard compiler intrinsics in no_std
compiler_builtins = { version = "0.1", features = ["mem"] }

[build-dependencies]
cc = "1.0"

[profile.release]
panic = "abort"
lto = true